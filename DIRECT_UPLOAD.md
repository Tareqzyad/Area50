# Area 50 — دليل الرفع المباشر

هذه الحزمة مخصصة لاستضافة تدعم **Node.js** وتشغّل خادماً مستمراً، مثل Render أو Railway أو استضافة cPanel التي توفر Node.js Application. الموقع ليس ملف HTML ثابتاً فقط؛ فهو يتضمن خادم Express وواجهة tRPC وقاعدة بيانات MySQL لحفظ الحجوزات والأسعار.

## ماذا ترفع؟

ارفع ملف `area50-direct-upload.zip`، ثم فك ضغطه في مجلد المشروع. الحزمة لا تحتوي على `node_modules` ولا على ملفات `.env` أو أي أسرار محلية. لذلك يجب على الاستضافة تثبيت الاعتماديات أثناء عملية البناء.

| الإعداد | القيمة أو الأمر |
|---|---|
| Install / Build | `npm install && npm run build` أو `pnpm install --frozen-lockfile && pnpm build` |
| Start | `npm start` أو `pnpm start` |
| Node.js | الإصدار 20 أو أحدث يفضل أن يكون متاحاً |
| Port | لا تضع رقماً ثابتاً؛ الخادم يقرأ المتغير `PORT` من الاستضافة |
| Database | MySQL أو TiDB متاح من الإنترنت |

## متغيرات البيئة

أضف هذه المتغيرات في قسم **Environment Variables / Secrets** لدى مزود الاستضافة:

| المتغير | مطلوب | الغرض |
|---|---:|---|
| `DATABASE_URL` | نعم | رابط اتصال قاعدة بيانات MySQL، مثال: `mysql://user:password@host:3306/database` |
| `JWT_SECRET` | نعم | مفتاح عشوائي طويل لتوقيع جلسات الخادم |
| `AREA50_ADMIN_CODE` | نعم | رمز دخول لوحة الإدارة؛ القيمة الحالية هي `area50iq119080`، ويمكن تغييره إلى رمزك الخاص |

الصور الموجودة في النسخة الخارجية مضمّنة داخل الحزمة، لذلك لا تحتاج إلى مفاتيح Manus للتخزين حتى تظهر الواجهة. خصائص Manus OAuth الاختيارية غير مطلوبة لتشغيل الحجوزات ولوحة الإدارة الحالية. إذا رغبت لاحقاً في تفعيل تسجيل الدخول عبر Manus أو وظائف التخزين السحابية، أضف المتغيرات التي يوفرها حساب Manus، ومنها `VITE_OAUTH_PORTAL_URL` و`VITE_APP_ID` و`OAUTH_SERVER_URL` و`BUILT_IN_FORGE_API_URL` و`BUILT_IN_FORGE_API_KEY`.

## قاعدة البيانات

بعد إنشاء قاعدة MySQL ووضع `DATABASE_URL`، شغّل أمر تهيئة الجداول من مجلد المشروع:

```bash
npm run db:push
```

أو:

```bash
pnpm db:push
```

إذا كانت الاستضافة تمنع تشغيل أوامر CLI بعد النشر، شغّل الأمر محلياً أو من Shell الاستضافة مرة واحدة قبل فتح الموقع. لا تحذف جداول `bookings` أو `room_prices` إذا كانت تحتوي على حجوزات محفوظة.

## إعداد الخدمة

في Render أو Railway، أنشئ **Web Service** من ملف ZIP أو من مجلد المشروع بعد فك الضغط، ثم استخدم أمر البناء `npm install && npm run build` وأمر التشغيل `npm start`. في cPanel، أنشئ تطبيق Node.js، اختر مجلد المشروع، نفّذ تثبيت الحزم، ثم اضبط أمر التشغيل على `npm start`. يجب أن تكون قاعدة البيانات قابلة للوصول من خادم الاستضافة، وليس من جهازك المحلي فقط.

## الوصول إلى الموقع ولوحة الإدارة

بعد نجاح التشغيل افتح النطاق الرئيسي للموقع. للوصول إلى لوحة الإدارة أضف المسار التالي إلى نهاية النطاق:

```text
/area50iq-admin
```

مثال:

```text
https://your-domain.com/area50iq-admin
```

أدخل قيمة `AREA50_ADMIN_CODE` التي وضعتها في إعدادات الاستضافة. لا تضع رمز الإدارة داخل ملفات الواجهة أو داخل `README` عام إذا كنت تستخدم رمزاً جديداً.

## ملاحظات مهمة

هذه الحزمة لا تتضمن قاعدة البيانات نفسها؛ فهي تتضمن مخطط Drizzle وكود التهيئة فقط. يجب إنشاء قاعدة MySQL على مزود الاستضافة وإضافة رابطها إلى `DATABASE_URL`. كما أن النسخة المضغوطة لا تتضمن `node_modules` لأن الاستضافة يجب أن تثبت الاعتماديات من `package.json` و`pnpm-lock.yaml` أثناء البناء.
