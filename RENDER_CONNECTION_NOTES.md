# ملاحظات ربط Render الحالية

- مستودع المشروع هو: `https://github.com/Tareqzyad/Area50` وهو منشور حالياً كـ Public.
- ظهر المستودع فارغاً في البداية ثم تم رفع فرع `main` والتحقق من وصوله إلى GitHub.
- حساب Render المرتبط للمستخدم يعرض مستودع `Shop` فقط ضمن Git Provider، لذلك يحتاج المستخدم لمنح تطبيق Render الوصول إلى مستودع `Area50` من إعدادات GitHub App قبل المتابعة.
- تم إرشاد المستخدم لفتح `https://github.com/settings/installations` ثم Configure لتطبيق Render واختيار المستودع `Area50` ضمن Repository access.

## نتيجة التحقق من Render

توضح وثائق Render أن **Web Service** يدعم تطبيقات Node.js وExpress، وأن اختيار نوع المثيل **Free** ينشر الخدمة بلا رسوم ولا يحتاج بطاقة. البطاقة ظهرت لأن المستخدم وصل إلى خيار مثيل مدفوع، ولذلك يجب إلغاء نافذة البطاقة والعودة إلى نموذج إعداد الخدمة ثم اختيار **Free**. تستخدم الخطة المجانية خدمة تنام بعد 15 دقيقة من عدم النشاط، وتستيقظ عند أول زيارة لاحقة.

المصادر: `https://render.com/docs/free` و`https://render.com/docs/your-first-deploy` و`https://render.com/docs/web-services`.

## حالة إعداد الخدمة

تم الوصول إلى نموذج خدمة `Area50` في Render مع مصدر `Tareqzyad / Area50`. اختار المستخدم مثيل **Free** الظاهر بقيمة **$0 / month** وموارد 512 MB RAM و0.1 CPU. أوامر البناء والتشغيل المعروضة في النموذج متوافقة مع دليل المشروع: تثبيت الحزم وبناء التطبيق، ثم `pnpm run start`.
