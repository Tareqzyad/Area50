# مهام لوحة إدارة Area 50

- [x] ترقية المشروع إلى نسخة كاملة مع قاعدة بيانات وواجهة خادم.
- [x] إضافة خانة اسم الشخص الحاجز إلى VIP وVVIP.
- [x] حفظ حجوزات VIP وVVIP فعلياً مع التاريخ والوقت الكامل وعدد الأشخاص.
- [x] إنشاء صفحة إدارة مخفية ومحمية برمز المدير.
- [x] عرض الحجوزات داخل لوحة الإدارة مع حالة الحجز.
- [x] تمكين المدير من تعديل أسعار VIP وVVIP من اللوحة.
- [x] حماية إجراءات الإدارة وعدم كشف الرمز داخل الواجهة العامة.
- [x] اختبار الحجز ولوحة الإدارة على الهاتف وسطح المكتب.
- [x] حفظ نسخة checkpoint بعد التحقق.

## نتائج المراجعة البصرية

صفحة `/center` تعرض اسم الحاجز داخل نموذجي VIP وVVIP، والساعات منفصلة كبداية ونهاية كاملة. صفحة `/area50iq-admin` تعرض شاشة دخول خاصة وغير مرتبطة بالتنقل العام. على الهاتف ظهرت صفحة السنتر والنماذج مرتبة عمودياً وقابلة للقراءة. ملاحظة: معاينة لوحة الإدارة على العرض الضيق تركز البطاقة في الجهة اليمنى ضمن لقطة الصفحة الكاملة، لكن الحقول نفسها تبقى ضمن التصميم المتجاوب.

## تحقق إضافي مطلوب قبل التسليم

- [x] اختبار حفظ حجز VIP/VVIP فعلياً ثم قراءته من لوحة الإدارة.
- [x] اختبار عرض الحجوزات الحقيقية وتحديث حالات pending/confirmed/cancelled.
- [x] اختبار حفظ أسعار VIP/VVIP ثم انعكاسها على صفحة السنتر بعد إعادة الجلب.
- [x] اختبار رفض إجراءات الإدارة غير المصرح بها ومراجعة عدم تسرب رمز الإدارة إلى كود العميل.

## تغطية نهائية إضافية

- [x] اختبار تكاملي فعلي لحجز VVIP ثم ظهوره في لوحة الإدارة.
- [x] اختبار تحديث الحالة إلى cancelled ضمن الحجوزات المحفوظة.
- [x] إثبات انعكاس سعري VIP وVVIP في استعلامات صفحة السنتر بعد إعادة الجلب.
- [x] اختبار كل إجراءات الإدارة الحساسة ومراجعة bundle العميل بحثاً عن رمز الإدارة.

## حزمة الرفع المباشر الخارجي

- [x] إصلاح والتحقق من مسار تشغيل الخادم في بيئة Node.js خارج Manus.
- [x] بناء نسخة الإنتاج والتأكد من إنشاء `dist`.
- [x] إنشاء ZIP نظيف دون `node_modules` أو ملفات `.env` أو أسرار محلية.
- [x] فحص محتويات ZIP وإرفاقه للمستخدم مع تعليمات المتغيرات المطلوبة.
- [x] إرفاق ملف `/home/ubuntu/area50-direct-upload.zip` للمستخدم مع تعليمات المتغيرات المطلوبة ومسار لوحة الإدارة.

## تطوير صفحة المتجر

- [x] مراجعة صفحة Store الحالية وبنية التطبيق وقاعدة البيانات قبل التعديل.
- [x] تحديد نموذج المنتجات والتصنيفات والأسعار وحالة التوفر.
- [x] تصميم واجهة متجر عربية RTL متجاوبة مع بطاقات المنتجات والتصفية.
- [x] إضافة سلة مشتريات وتحديث الكميات وإجمالي الطلب.
- [x] ربط المنتجات والتصنيفات بخادم tRPC وقاعدة البيانات؛ سلة الطلب محلية لحين إضافة checkout.
- [x] إضافة اختبارات Vitest لمسارات إدارة المتجر وصلاحيات الأدمن.
- [x] التحقق البصري والوظيفي ثم حفظ checkpoint.

## إدارة المتجر من لوحة الأدمن
- [x] إضافة جداول `store_categories` و `store_products` في قاعدة البيانات عبر Drizzle schema وتنفيذ الهجرة.
- [x] إنشاء إجراءات tRPC عامة لعرض التصنيفات والمنتجات المحفوظة، وإجراءات محمية للأدمن (إضافة، تعديل، حذف، تغيير التوفر، وإدارة الصور).
- [x] تطوير واجهة لوحة الأدمن لإدارة التصنيفات والمنتجات بشكل كامل (نماذج إضافة وتعديل مع رفع الصور أو إدخال الروابط والأسعار والأوصاف).
- [x] تحديث صفحة المتجر `/store` لجلب البيانات الحقيقية من الخادم وتوفير التصفية حسب التصنيف وعرض سلة التسوق.
- [x] كتابة اختبارات Vitest لإجراءات المنتجات والتصنيفات وصلاحيات الأدمن.
- [x] اختبار التشغيل الكامل وحفظ checkpoint جديد.

## معالجة فجوات التحقق الأخيرة

- [x] منع إضافة المنتجات غير المتوفرة أو ذات المخزون صفر، وتقييد كمية السلة بسقف المخزون.
- [x] إضافة حالة خطأ واضحة عند فشل تحميل تصنيفات أو منتجات المتجر.
- [x] إضافة اختبارات لمسارات المتجر العامة وعمليات CRUD الخاصة بالأدمن والتحقق من المدخلات.

## اعتماد نسخة المتجر النهائية

- [x] حفظ checkpoint جديد بعد تعديلات المتجر وإدارة الأدمن.
- [x] إعادة التحقق من `/store` و`/area50iq-admin` على النسخة المحفوظة الجديدة.

## نظام طلبات المتجر المباشر إلى لوحة الأدمن

- [x] إضافة جداول `store_orders` و `store_order_items` في قاعدة البيانات عبر Drizzle schema.
- [x] تشغيل الهجرة وتطبيق جداول الطلبات على قاعدة البيانات MySQL.
- [x] إضافة إجراءات tRPC لإنشاء الطلب من صفحة المتجر وعرض الطلبات وتحديث حالتها في لوحة الأدمن.
- [x] تطوير نموذج إتمام الطلب في سلة المتجر (الاسم، الهاتف، العنوان، الملاحظات) وإرسال الطلب.
- [x] إضافة قسم "طلبات المتجر" داخل لوحة الأدمن مع تفاصيل الزبون والمنتجات وحالات الطلب.
- [x] كتابة واجتياز اختبارات Vitest لنظام الطلبات وصلاحيات الأدمن.

## إصلاح واجهة أدمن الطلبات والتحقق النهائي

- [x] إصلاح استجابة صفحة `/area50iq-admin` على الهاتف بحيث تبقى بطاقة الدخول كاملة داخل viewport.
- [x] إعادة التحقق بصرياً من `/area50iq-admin` وقسم طلبات المتجر على الهاتف وسطح المكتب بعد الإصلاح.
- [x] حفظ checkpoint جديد بعد إصلاح واجهة الأدمن الخاصة بنظام طلبات المتجر.
