# مهام لوحة إدارة Area 50

- [x] ترقية المشروع إلى نسخة كاملة مع قاعدة بيانات وواجهة خادم.
- [x] إضافة خانة اسم الشخص الحاجز إلى VIP وVVIP.
- [x] حفظ حجوزات VIP وVVIP فعلياً مع التاريخ والوقت الكامل وعدد الأشخاص.
- [x] إنشاء صفحة إدارة مخفية ومحمية برمز المدير.
- [x] عرض الحجوزات داخل لوحة الإدارة مع حالة الحجز.
- [x] تمكين المدير من تعديل أسعار VIP وVVIP من اللوحة.
- [x] حماية إجراءات الإدارة وعدم كشف الرمز داخل الواجهة العامة.
- [x] اختبار الحجز ولوحة الإدارة على الهاتف وسطح المكتب.
- [x] حفظ نسخة checkpoint بعد التحقق.

## نتائج المراجعة البصرية

صفحة `/center` تعرض اسم الحاجز داخل نموذجي VIP وVVIP، والساعات منفصلة كبداية ونهاية كاملة. صفحة `/area50iq-admin` تعرض شاشة دخول خاصة وغير مرتبطة بالتنقل العام. على الهاتف ظهرت صفحة السنتر والنماذج مرتبة عمودياً وقابلة للقراءة. ملاحظة: معاينة لوحة الإدارة على العرض الضيق تركز البطاقة في الجهة اليمنى ضمن لقطة الصفحة الكاملة، لكن الحقول نفسها تبقى ضمن التصميم المتجاوب.

## تحقق إضافي مطلوب قبل التسليم

- [x] اختبار حفظ حجز VIP/VVIP فعلياً ثم قراءته من لوحة الإدارة.
- [x] اختبار عرض الحجوزات الحقيقية وتحديث حالات pending/confirmed/cancelled.
- [x] اختبار حفظ أسعار VIP/VVIP ثم انعكاسها على صفحة السنتر بعد إعادة الجلب.
- [x] اختبار رفض إجراءات الإدارة غير المصرح بها ومراجعة عدم تسرب رمز الإدارة إلى كود العميل.

## تغطية نهائية إضافية

- [x] اختبار تكاملي فعلي لحجز VVIP ثم ظهوره في لوحة الإدارة.
- [x] اختبار تحديث الحالة إلى cancelled ضمن الحجوزات المحفوظة.
- [x] إثبات انعكاس سعري VIP وVVIP في استعلامات صفحة السنتر بعد إعادة الجلب.
- [x] اختبار كل إجراءات الإدارة الحساسة ومراجعة bundle العميل بحثاً عن رمز الإدارة.

## حزمة الرفع المباشر الخارجي

- [x] إصلاح والتحقق من مسار تشغيل الخادم في بيئة Node.js خارج Manus.
- [x] بناء نسخة الإنتاج والتأكد من إنشاء `dist`.
- [x] إنشاء ZIP نظيف دون `node_modules` أو ملفات `.env` أو أسرار محلية.
- [x] فحص محتويات ZIP وإرفاقه للمستخدم مع تعليمات المتغيرات المطلوبة.
- [x] إرفاق ملف `/home/ubuntu/area50-direct-upload.zip` للمستخدم مع تعليمات المتغيرات المطلوبة ومسار لوحة الإدارة.

## تطوير صفحة المتجر

- [x] مراجعة صفحة Store الحالية وبنية التطبيق وقاعدة البيانات قبل التعديل.
- [x] تحديد نموذج المنتجات والتصنيفات والأسعار وحالة التوفر.
- [x] تصميم واجهة متجر عربية RTL متجاوبة مع بطاقات المنتجات والتصفية.
- [x] إضافة سلة مشتريات وتحديث الكميات وإجمالي الطلب.
- [x] ربط المنتجات والتصنيفات بخادم tRPC وقاعدة البيانات؛ سلة الطلب محلية لحين إضافة checkout.
- [x] إضافة اختبارات Vitest لمسارات إدارة المتجر وصلاحيات الأدمن.
- [x] التحقق البصري والوظيفي ثم حفظ checkpoint.

## إدارة المتجر من لوحة الأدمن
- [x] إضافة جداول `store_categories` و `store_products` في قاعدة البيانات عبر Drizzle schema وتنفيذ الهجرة.
- [x] إنشاء إجراءات tRPC عامة لعرض التصنيفات والمنتجات المحفوظة، وإجراءات محمية للأدمن (إضافة، تعديل، حذف، تغيير التوفر، وإدارة الصور).
- [x] تطوير واجهة لوحة الأدمن لإدارة التصنيفات والمنتجات بشكل كامل (نماذج إضافة وتعديل مع رفع الصور أو إدخال الروابط والأسعار والأوصاف).
- [x] تحديث صفحة المتجر `/store` لجلب البيانات الحقيقية من الخادم وتوفير التصفية حسب التصنيف وعرض سلة التسوق.
- [x] كتابة اختبارات Vitest لإجراءات المنتجات والتصنيفات وصلاحيات الأدمن.
- [x] اختبار التشغيل الكامل وحفظ checkpoint جديد.

## معالجة فجوات التحقق الأخيرة

- [x] منع إضافة المنتجات غير المتوفرة أو ذات المخزون صفر، وتقييد كمية السلة بسقف المخزون.
- [x] إضافة حالة خطأ واضحة عند فشل تحميل تصنيفات أو منتجات المتجر.
- [x] إضافة اختبارات لمسارات المتجر العامة وعمليات CRUD الخاصة بالأدمن والتحقق من المدخلات.

## اعتماد نسخة المتجر النهائية

- [x] حفظ checkpoint جديد بعد تعديلات المتجر وإدارة الأدمن.
- [x] إعادة التحقق من `/store` و`/area50iq-admin` على النسخة المحفوظة الجديدة.

## نظام طلبات المتجر المباشر إلى لوحة الأدمن

- [x] إضافة جداول `store_orders` و `store_order_items` في قاعدة البيانات عبر Drizzle schema.
- [x] تشغيل الهجرة وتطبيق جداول الطلبات على قاعدة البيانات MySQL.
- [x] إضافة إجراءات tRPC لإنشاء الطلب من صفحة المتجر وعرض الطلبات وتحديث حالتها في لوحة الأدمن.
- [x] تطوير نموذج إتمام الطلب في سلة المتجر (الاسم، الهاتف، العنوان، الملاحظات) وإرسال الطلب.
- [x] إضافة قسم "طلبات المتجر" داخل لوحة الأدمن مع تفاصيل الزبون والمنتجات وحالات الطلب.
- [x] كتابة واجتياز اختبارات Vitest لنظام الطلبات وصلاحيات الأدمن.

## إصلاح واجهة أدمن الطلبات والتحقق النهائي

- [x] إصلاح استجابة صفحة `/area50iq-admin` على الهاتف بحيث تبقى بطاقة الدخول كاملة داخل viewport.
- [x] إعادة التحقق بصرياً من `/area50iq-admin` وقسم طلبات المتجر على الهاتف وسطح المكتب بعد الإصلاح.
- [x] حفظ checkpoint جديد بعد إصلاح واجهة الأدمن الخاصة بنظام طلبات المتجر.

## توحيد وجهة الطلبات والحجوزات إلى لوحة الأدمن (إلغاء واتساب)

- [x] فحص صفحة Center.tsx وإزالة أي روابط أو توجيهات لفتح واتساب عند إتمام حجز VIP أو VVIP.
- [x] التأكد من أن حجز الغرف يُرسل بيانات الحجز إلى الخادم وتحفظ في قاعدة البيانات لتظهر فوراً في لوحة الأدمن.
- [x] تحديث رسائل النجاح والواجهة لتعلم الزبون أن حجزه تم إرساله إلى إدارة المركز بنجاح.
- [x] تشغيل اختبارات المشروع وبناء الإنتاج للتحقق من سلامة التعديل.
- [x] حفظ checkpoint جديد للموقع المنشور.

## نظام 4 غرف VIP وVVIP، منع التعارض، وحذف الحجوزات الملغاة

- [x] تحديث مخطط Drizzle وجداول الحجوزات لإضافة حقل رقم الغرفة (`room_number`: 1 إلى 4).
- [x] إضافة تحقق في الخادم لمنع حجز نفس رقم الغرفة في نفس اليوم والتاريخ والأوقات المتداخلة.
- [x] تطوير واجهة الحجز في Center.tsx لتشمل اختيار رقم الغرفة (من 1 إلى 4) لكل من VIP وVVIP.
- [x] إضافة إجراءات أدمن لحذف الحجوزات والطلبات الملغاة من قاعدة البيانات نهائياً عند ضغط زر الإلغاء.
- [x] تحديث واجهة لوحة الأدمن لعرض رقم الغرفة وأزرار الحذف الفوري للطلبات والحجوزات.
- [x] كتابة واجتياز اختبارات Vitest للتحقق من منع التعارض وحذف الحجوزات.
- [x] بناء الإنتاج وحفظ checkpoint جديد للموقع المنشور.

## نظام 4 غرف VIP وVVIP، منع التعارض، وحذف الحجوزات الملغاة

- [x] تحديث مخطط Drizzle وجداول الحجوزات لإضافة حقل رقم الغرفة (`room_number`: 1 إلى 4).
- [x] إضافة تحقق في الخادم لمنع حجز نفس رقم الغرفة في نفس اليوم والتاريخ والأوقات المتداخلة.
- [x] تطوير واجهة الحجز في Center.tsx لتشمل اختيار رقم الغرفة (من 1 إلى 4) لكل من VIP وVVIP.
- [x] إضافة إجراءات أدمن لحذف الحجوزات والطلبات الملغاة من قاعدة البيانات نهائياً عند ضغط زر الإلغاء.
- [x] تحديث واجهة لوحة الأدمن لعرض رقم الغرفة وأزرار الحذف الفوري للطلبات والحجوزات.
- [x] كتابة واجتياز اختبارات Vitest للتحقق من منع التعارض وحذف الحجوزات.
- [x] بناء الإنتاج وحفظ checkpoint جديد للموقع المنشور.

## زر تعديل عدد الغرف من لوحة الأدمن

- [x] إضافة إعدادات عدد الغرف إلى قاعدة البيانات (جدول settings أو ما يشابهه).
- [x] إنشاء إجراءات tRPC لقراءة وتحديث عدد غرف VIP وVVIP من لوحة الأدمن.
- [x] إضافة قسم "إعدادات الغرف" في Admin.tsx لتعديل عدد الغرف.
- [x] تحديث واجهة الحجز في Center.tsx لجلب العدد المسموح للغرف ديناميكياً وعرض الخيارات (من 1 إلى العدد المحدث).
- [x] تحديث التحقق في الخادم للتأكد من أن رقم الغرفة المحجوز لا يتجاوز العدد الحالي للإعدادات.
- [x] اختبار التشغيل وبناء الإنتاج وحفظ checkpoint.
- [x] إضافة أو تحديث اختبارات Vitest تغطي تعارض نفس الغرفة، السماح بغرفة أو وقت مختلف، والحذف النهائي للحجز أو طلب المتجر عند الإلغاء.

## فجوات التحقق قبل اعتماد إعدادات الغرف

- [x] إضافة حالات Vitest صريحة للسماح بالحجز عند اختلاف رقم الغرفة في نفس الوقت.
- [x] إضافة حالة Vitest صريحة للسماح بالحجز في نفس الغرفة عند اختلاف الوقت دون تداخل.
- [x] حفظ checkpoint جديد بعد تعديلات إعدادات عدد الغرف، ثم إعادة التحقق من `/center` و`/area50iq-admin` على النسخة المحفوظة.

## توثيق اعتماد إعدادات الغرف

- [x] حفظ checkpoint جديد بعد تعديل اختبارات إعدادات الغرف.
- [x] إعادة التحقق من `/center` و`/area50iq-admin` بعد حفظ checkpoint الجديد وتوثيق أن الفحص تم على النسخة المحفوظة.

## تجهيز النشر الخارجي عبر GitHub

- [x] إضافة دليل رفع وتشغيل المشروع على GitHub ومنصة استضافة خارجية.
- [x] مراجعة دليل النشر للتأكد من عدم تضمين أسرار أو بيانات دخول فعلية.
- [x] تشغيل الاختبارات والبناء بعد إضافة دليل النشر.
- [x] حفظ checkpoint لتجهيز النشر الخارجي.

## ربط المستودع الذي زوّد به المستخدم

- [x] ربط المستودع المحلي بـ `https://github.com/Tareqzyad/Area50.git` كوجهة GitHub.
- [x] رفع النسخة المعتمدة إلى فرع `main` في GitHub.
- [x] التحقق من وصول آخر commit إلى المستودع البعيد.

## طريقة مفتاح النشر البديلة لـ GitHub

- [x] إنشاء مفتاح نشر مخصص وآمن لمستودع Area50.
- [x] إضافة المفتاح العام إلى إعدادات المستودع مع إذن الكتابة.
- [x] دفع النسخة المعتمدة والتحقق من ظهورها في GitHub.

## إعداد الاستضافة الخارجية عبر Render

- [ ] إكمال ربط مستودع Area50 وإنشاء خدمة Render Web Service مع رابط قابل للتحقق.
- [x] اختيار مثيل Render Free دون بطاقة قبل إنشاء الخدمة.
- [ ] ضبط أوامر البناء والتشغيل ومتغيرات البيئة المطلوبة.
- [ ] متابعة حالة البناء الأولي والتحقق من رابط الخدمة.
- [ ] تهيئة قاعدة البيانات والتحقق من عمل الموقع ولوحة الإدارة خارج Manus.
- [ ] توثيق إعدادات خدمة Render ورابطها بعد اكتمال الإنشاء.

## حاجز Render وخيار الاستضافة البديل

- [x] توثيق أن Render طلب بطاقة رغم اختيار Free وعدم إكمال النشر المدفوع.
- [x] مقارنة مسارات استضافة لا تطلب بطاقة وتحافظ على خادم Area 50 وقاعدة البيانات.
- [x] اختيار Cloudflare Workers + D1 كمسار بديل مجاني دون بطاقة.
- [x] تكييف قاعدة البيانات من MySQL إلى D1/SQLite مع الحفاظ على الحجوزات والمتجر ولوحة الأدمن.
- [x] تكييف نقطة تشغيل API وملفات الواجهة لتعمل كـ Cloudflare Worker.
- [x] إضافة دعم رفع صور المنتجات عبر Cloudinary Free، مع بقاء الربط الفعلي بانتظار بيانات حساب المستخدم.
- [x] إضافة إعدادات Cloudflare وإرشادات نشر مبسطة وربطها بمستودع GitHub.
- [x] تشغيل الاختبارات والبناء والتحقق المحلي من النسخة المجهزة للنشر.
- [x] اختبار صفحات `/center` و`/store` و`/area50iq-admin` محلياً من خلال Cloudflare Worker.
- [x] اختبار CRUD للتصنيفات والمنتجات وطلب متجر وتحديثه وحذفه داخل قاعدة D1 محلياً.
- [ ] إنشاء حسابي Cloudflare وCloudinary المجانيين ثم إضافة أسرارهما دون بطاقة.
- [ ] إنشاء قاعدة D1 ونشر Worker وربط رابط الموقع الخارجي.
- [ ] اختبار الموقع ولوحة الأدمن والحجز والطلب بعد النشر الخارجي.

## بديل الاستضافة دون بطاقة

- [x] تقييم منصة استضافة مجانية متوافقة مع خادم Node وقاعدة بيانات Area 50 ولا تطلب بطاقة؛ خدمة Render Web المجانية مناسبة عند اختيار Free.
- [ ] إعداد مسار نشر بديل وإرشاد المستخدم لتشغيله.

## منصة بديلة عن Render وNetlify

- [ ] مقارنة منصات بديلة تستضيف خادم Node.js وقاعدة MySQL لتطبيق Area 50.
- [ ] اختيار منصة مناسبة وتجهيز إعدادات النشر لها.
- [ ] إرشاد المستخدم لإكمال النشر والتحقق من الرابط الخارجي.
